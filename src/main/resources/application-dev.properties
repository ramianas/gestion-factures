# Fichier: src/main/resources/application-dev.properties

# === Database Configuration ===
spring.datasource.url=jdbc:postgresql://localhost:5432/facture_db
spring.datasource.username=postgres
spring.datasource.password=anasl9ray
spring.datasource.driver-class-name=org.postgresql.Driver

# === JPA / Hibernate Configuration ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database=postgresql
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# === Logging Configuration ===
logging.level.ma.eai.daf.facture=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG

# === SMTP Gmail ===
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=anasramioffice@gmail.com
spring.mail.password=gool hajf eqql kgsc
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# === JWT Configuration ===
jwt.secret=mySecretKey123456789012345678901234567890123456789012345678901234567890
jwt.expiration=86400000
# 86400000ms = 24 heures

# === Custom Application Upload Directory ===
application.file.uploads.photos-output-path=./uploads

# === Server Configuration ===
server.port=8088
server.servlet.context-path=/

# === Security Configuration ===
# Désactiver la sécurité par défaut de Spring Boot
spring.security.user.name=
spring.security.user.password=

# === Management Endpoints ===
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized